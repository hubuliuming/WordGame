# Cocos 代码规范

## 代码结构清晰

- 写入代码时必须保持职责边界清晰
- 不允许把多个职责堆在同一个脚本中
- 当实现包含多个独立职责、流程阶段或可复用能力时，应适当拆分为多个脚本
- 拆分脚本必须服务于已确认方案和现有结构，不得借机重构系统或扩展无关模块
- 新增 `Component`、服务类、工具类或配置文件前，必须确认它们属于已确认方案范围

## 失败暴露优先

- 禁止为了“防止报错”添加防御式冗余校验
- 必需依赖缺失必须暴露错误，便于发现资源绑定、初始化顺序或调用链问题；独立批量条目按下文“批量处理与单项失败隔离”记录错误并跳过当前项，其余场景不得静默吞掉错误
- 不允许用 `null` 校验吞掉必需依赖错误
- 不允许用 `Find`递归节点搜索等查找 API 兜底必需节点
- 不允许用临时动态创建节点、组件、资源、空对象或默认配置兜底必需依赖
- 属性检查器、Prefab、Scene、文档或已确认方案定义为必需的字段，默认视为必须存在
- 外部输入、用户输入、存档数据、网络数据、异步加载结果、平台 API 返回值、远端配置或文档明确可选的对象属于必须校验范围；批量处理边界还允许对单项失败和整批必要前提进行必要校验
- 分析时若发现必需依赖来源或绑定不明确，应要求确认绑定、资源引用或调用链，不得自行补兜底逻辑；运行时独立条目的依赖错误按单项失败隔离规则处理，不中断后续条目

---

## 编写代码规范


### 性能优先与保护性代码规则

- 在保证行为正确、职责边界清晰和已确认需求成立的前提下，性能优先，优先选择更少分配、更少遍历、更少查找和更低调用开销的实现
- 禁止为未经证实的异常路径堆叠 `null` 判断、重复校验、无意义的 `try/catch`、默认值兜底、对象复制或集合转换
- 保护性检查只用于外部输入、文档明确可选的对象或已经证实的风险，并应在责任边界统一处理，避免在调用链各层重复防护
- 下文批量处理所需的单项失败隔离和整批必要前提校验属于必要处理，允许在对应边界使用校验和 `try/catch`，不视为冗余保护；不得在每层调用中重复添加
- 性能优化不得牺牲正确性或改变已确认行为；无法确认风险时应先暴露问题并请求确认，不得自行增加兜底代码

### 批量处理与单项失败隔离

- 适用于背包 item 加载、批量目标实例化、列表元素初始化等条目相互独立的流程：单项数据错误、资源加载失败、实例化失败或初始化异常，只终止当前项，必须记录错误并继续处理后续条目
- 可直接判断的单项错误使用“错误日志 + `continue`”；单项处理封装在函数或回调中时，可返回明确的失败结果，由批量调用方继续下一项，不得退出整个批次
- 可能抛异常的加载、实例化和初始化操作，应在单项处理边界使用 `try/catch`，记录原始异常后继续下一项；禁止仅在整个循环外包裹 `try/catch`，导致某项失败后剩余条目不再执行
- 异步加载同样必须逐项隔离失败：单项 Promise 拒绝或回调报错不得中断其他条目的加载和结果处理；并发流程应逐项处理失败或使用 `Promise.allSettled` 等方式收集结果，不得仅依赖裸 `Promise.all` 的整批成功路径
- 单项内部的必需依赖缺失也必须记录错误并跳过该项，不得静默吞掉，或使用空对象、默认配置、临时节点等将失败项伪装成成功
- 实例化成功但初始化失败时，必须清理当前项已创建的节点、已注册的监听及其他已持有资源，避免残留半成品；清理失败也应记录错误并隔离，不得阻断后续条目，且不得释放其他条目仍在使用的共享资源
- 只有初始化成功的对象才能计入成功数量、加入有效列表或登记有效缓存；失败项不得作为有效结果参与后续处理
- 使用项目现有错误日志接口，至少包含模块、处理阶段、条目标识（如 itemId 或条目索引）、资源路径（适用时）和原始异常（存在时），确保能定位具体失败对象
- 同一个错误由负责隔离的边界记录一次，避免调用链各层重复打印；禁止仅输出缺少对象标识和原因的泛化错误日志
- 整批共用的必要前提失效（如列表容器或公共初始化服务缺失）时，应在批次入口记录错误并终止本次流程，避免每个条目重复失败
- 要求整体一致性的操作（如扣费与发奖）不适用逐项跳过规则，应遵循已确认的整体失败处理方案

